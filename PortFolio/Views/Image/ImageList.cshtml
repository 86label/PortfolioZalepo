@using System.IO;
@{
    ViewBag.Title = "ImageList";
}

<h2>ImageList</h2>

@*ViewBag.ImageNames[1]*@

<br />

@for (int i = 0; i < ViewBag.FN.Length; i++)
{
    <img src="@ViewBag.ImageRP@Path.GetFileName(ViewBag.FN[i])" style="padding: 5px; max-width: 90%" />
}
